# 猪猪侠日程提醒 · 手机桌面挂件 — 使用说明

一套「猪猪侠」风格的每日定时提醒桌面挂件，包含两份产物：

| 产物 | 是什么 | 适合谁 |
| --- | --- | --- |
| **原型预览.html** | 手机外观与交互的高保真演示，双击即可在浏览器打开 | 想先看效果、随手试用 |
| **安卓工程 PigHeroReminder** | 可编译安装的安卓原生应用，含真正的桌面小组件 | 安卓手机，想要到点真实弹通知 |

> 形象说明：挂件小猪为粉丝风格的手绘矢量形象（红色英雄面罩、黄色战衣、披风），**仅供个人学习与自用，请勿商用或公开分发**。

---

## 一、先玩原型（零门槛）

1. 双击打开 `原型预览.html`（电脑、手机浏览器均可）。
2. 页面中央是一台模拟手机，桌面上就是猪猪侠挂件，显示实时时钟和「下一条提醒」。
3. **点挂件** → 进入设置页：可添加 / 开关 / 删除任意时间点的提醒。
4. 点「立即试播一次提醒效果」可预览到点时的顶部通知和小猪喊话（带提示音）。
5. 数据保存在浏览器本地，每天循环；但网页关闭后无法保证后台提醒，**真机常驻提醒请安装下面的安卓版**。

---

## 二、安装安卓真机版（真正的桌面挂件）

### 你需要
- 一台 **Android 8.0（API 26）或更高**的安卓手机（近年手机基本都满足）。
- 一台电脑，安装 **Android Studio**（官方免费：developer.android.com/studio；国内可从安卓中文网等镜像下载）。安装时勾选默认的 Android SDK 与 JDK（JDK 17）即可，无需另装 Java。

### 编译安装（任选一种）

**方式 A：用数据线直接安装（推荐）**
1. 手机开启「开发者选项」→ 打开「USB 调试」（设置里连点版本号 7 次可开启开发者选项），数据线连电脑并允许调试。
2. Android Studio 选 **Open**，打开本工程的 `PigHeroReminder` 文件夹。
3. 首次打开会自动下载 Gradle 8.9 与 Android 插件（需联网，约几分钟），等待右下角同步完成。
4. 顶部设备列表选中你的手机，点绿色三角 **Run**，应用即自动安装。

**方式 B：生成 APK 安装包**
1. Android Studio 打开工程同步完成后，菜单 **Build → Build Bundle(s) / APK(s) → Build APK(s)**。
2. 生成的 APK 在 `app/build/outputs/apk/debug/app-debug.apk`，发到手机上点击安装（需允许「安装未知来源应用」）。

### 把挂件放到桌面
1. 回到手机桌面，**长按桌面空白处** → 点「窗口小工具 / 小组件 / 微件」（不同品牌叫法不同）。
2. 找到 **「猪猪侠提醒」**，长按拖到桌面空位（默认 4×2 尺寸，可拖拽边缘缩放）。
3. 首次添加会自动打开设置页：选时间、填要做的事、点「＋ 添加提醒」，最后点右上角「完成」。
4. 以后**点一下桌面上的挂件**（或桌面应用图标「猪猪侠提醒」）随时增删改提醒。

### 使用提示
- 首次使用请**允许通知权限**（Android 13+ 会弹窗询问）。
- 内置 4 条示例提醒（07:30 起床、12:00 午饭、18:00 晚饭、21:30 睡觉），可自由删改。
- 定时采用系统「闹钟级」机制（setAlarmClock），省电模式下也能准时响；手机重启后会自动恢复定时。
- **小米/红米、OPPO、vivo、华为等机型**若到点不提醒：到「设置 → 应用管理 → 猪猪侠提醒」里允许**自启动**、允许**后台运行**，电池管理设为「不限制 / 无限制」，并确认通知权限已开。
- 点设置页「立即试播一次提醒效果」可立即收到一条测试通知，确认通道正常。

---

## 三、iPhone 用户

iOS 不允许侧载第三方小组件工程（必须 Mac + Xcode + 苹果开发者账号，且与安卓工程完全不同）。本包未包含 iOS 版，iPhone 上可先用 `原型预览.html`（用 Safari 打开后可「添加到主屏幕」当快捷应用）。如确需 iOS 原生挂件，可在此基础上另行用 WidgetKit 开发。

---

## 四、工程结构与技术说明（给想改代码的人）

```
PigHeroReminder/
├── app/src/main/
│   ├── AndroidManifest.xml            # 挂件、接收器、权限注册
│   ├── java/com/pighero/reminder/
│   │   ├── Reminder.kt                # 提醒数据模型 + 下次触发时间
│   │   ├── ReminderStore.kt           # SharedPreferences + JSON 存储
│   │   ├── ReminderScheduler.kt       # AlarmManager.setAlarmClock 每日定时
│   │   ├── ReminderReceiver.kt        # 到点发通知并重排次日闹钟
│   │   ├── BootReceiver.kt            # 开机/更新/改时间后恢复定时
│   │   ├── NotificationHelper.kt      # 通知渠道与通知发送
│   │   ├── PigWidgetProvider.kt       # 桌面挂件（TextClock + 下条提醒）
│   │   └── WidgetConfigureActivity.kt # 提醒设置页
│   └── res/                           # 布局、小猪矢量图、主题等全部资源
└── build.gradle.kts …                 # Gradle 构建配置（零第三方依赖）
```

- Kotlin 编写，minSdk 26 / targetSdk 34，AGP 8.5.2 + Gradle 8.9 + JDK 17。
- 不依赖任何第三方库，全部使用 Android 框架 API。
- 想换形象：替换 `res/drawable/ic_pig_hero.xml`（挂件/通知大图）、`ic_pig_notification.xml`（通知栏白色剪影）、`ic_launcher_foreground.xml`（桌面图标）三个矢量图即可。
